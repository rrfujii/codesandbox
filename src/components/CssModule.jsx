import classes from "./CssModule.module.scss";

export const CssModules = () => {
  return (
    <div class={classes.container}>
      <p class={classes.p}>- CSS Modules -</p>
      <button>(#^^#)</button>
    </div>
  );
};
